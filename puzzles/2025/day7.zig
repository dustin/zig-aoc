const std = @import("std");
const aoc = @import("aoc");

// fn part1(alloc: std.mem.Allocator, filename: []const u8) !u32 {
//     const g = try aoc.grid.openFileGrid(alloc, filename);
//     var mg = g.mutable(alloc);
// }
